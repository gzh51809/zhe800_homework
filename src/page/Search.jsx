import React, {
    Component
} from 'react';

import {
    ScrollContainer
} from '../component/public';

class Search extends Component {
    render() {
        return (
            <ScrollContainer>
                搜索页
            </ScrollContainer>
        );
    }
}

export default Search;
